[package]
name = "day8"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "*", features = ["blocking", "json"] }
dotenv = "*"
regex = "*"
itertools = "*"

[dev-dependencies]
criterion = "*"

[[bench]]
name = "criterion"
harness = false
